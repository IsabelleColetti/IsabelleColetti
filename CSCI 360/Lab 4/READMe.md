A program written in IBM Assembly which performs mathematical computations with packed decimals. Reads in packed decimal input. Shifts and prints packed decimals with and without prefixed characters.
